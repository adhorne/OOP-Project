/**
 * The production facility will also create portable movie players. The main difference between
 * these and the audio players is that they contain screens.
 *
 * @author Austin Horne
 */

// Create an enum called MonitorType that will store
public enum MonitorType {
  LCD, LED
}
